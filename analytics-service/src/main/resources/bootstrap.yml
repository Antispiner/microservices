spring:
  application:
    name: analytics-service
  cloud:
    config:
      uri: http://localhost:8888
      fail-fast: true
      password: password
      username: user


  datasource:
    driver-class-name: org.postgresql.Driver
    username: postgres
    password: 1418
    url: jdbc:postgresql://localhost/analytic?currentSchema=analytic
    platform: analytic
    initialization-mode: always
    continue-on-error: true
  jpa:
    show-sql: true
    generate-ddl: true
    database: postgresql