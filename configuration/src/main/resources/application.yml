spring:
  cloud:
    config:
      server:
        native:
          search-locations: classpath:/shared
  profiles:
    active: native

  bootstrap:
    enabled: true

  security:
    user:
      password: password


server:
  port: 8888