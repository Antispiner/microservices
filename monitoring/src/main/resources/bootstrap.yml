spring:
  application:
    name: monitoring
  cloud:
    config:
      uri: http://localhost:8888
      fail-fast: true
      password: password
      username: user

eureka:
  client:
    fetchRegistry: false
    register-with-eureka: false

feign:
  hystrix:
    enabled: true